To run the code, open the terminal and cd to the current directory.

Type the following command without quotes:

"sh run.sh"

Three files will be generated:

xvel.csv : contains x-velocity
yvel.csv : contains y-velocity

